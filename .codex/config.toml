[features]
# Enable multi-agent support explicitly in your own approved configuration.
# multi_agent = true

[agents]
max_concurrent_threads_per_session = 6
max_depth = 1

[agents.explorer]
description = "Read-only codebase explorer for gathering evidence before changes are proposed."
config_file = "agents/explorer.toml"

[agents.reviewer]
description = "PR reviewer focused on correctness, security, and missing tests."
config_file = "agents/reviewer.toml"

[agents.docs_researcher]
description = "Documentation specialist that verifies APIs, framework behavior, and release notes."
config_file = "agents/docs-researcher.toml"

[agents.agency_orchestrator]
description = "Read-only, opt-in router for bounded Agency Agents Kanban pipelines."
config_file = "agents/agency-orchestrator.toml"
